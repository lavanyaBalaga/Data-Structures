#include<stdio.h>
int main()
{
    int i,j,n,a[10],c=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d/t",&a[i]);
    }
    for(i=0;i<n-1;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(a[i]==a[j])
              c=c+1;
        }
       
    }
    printf("%d",c);
}
